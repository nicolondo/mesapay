import { redirect } from "next/navigation";

/**
 * /comercial/hoy — Fase 2 stub.
 * Redirects to pipeline until the "Hoy" screen is implemented (Fase 2).
 */
export default function HoyPage() {
  redirect("/comercial/crm");
}
