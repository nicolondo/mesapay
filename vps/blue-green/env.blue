# Per-color override loaded by mesapay@blue.service.
# Lives at /opt/mesapay/shared/.env.blue on the VPS.
# Only the PORT differs between blue and green; everything else
# (database URL, secrets, etc.) is shared via .env.production.
PORT=3300
